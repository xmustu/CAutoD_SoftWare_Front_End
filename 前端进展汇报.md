# 前端进展汇报 (2026-04-01)

## 1. 测试基础设施与 TDD 落地

本轮为当前 React 18 补齐了最基础的测试基础设施：
- 依赖项：`vitest`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`
- 配置文件：新增 `src/test/setup.js` 和配置 `vite.config.js` 的 test block。
- `package.json` 加入了 `"test": "vitest"` 脚本。

**TDD 实施情况：**
- 首个测试用例：`src/components/__tests__/ConversationDisplay.test.jsx`。
- 我们验证了 `ConversationDisplay` 的消息过滤和 AI 消息类型推断行为。
  - **失败表现 (Red)：** 由于我们初次加载 DOM 断言对 ReactMarkdown 的 syntax highlighter 多节点生成没有完美匹配，触发了 text not found。
  - **通过表现 (Green)：** 梳理和 mock 测试基础设施确保单元测试隔离 ReactMarkdown，使得核心的 `displayMessages` 过滤（保留 user 消息且正确路由 `optimize` 和 `geometry`）顺利跑通，没有引入额外的心智负担。

## 2. 前端轻量化与 AiMessage 负载优化

结合 `pretext` 库的“分离测量缓存与热路径计算，规避无意义的高频 Reflow”的核心思想，我们排查了当前的大会话渲染逻辑，并落地了如下优化：

**已经落地的优化（Refactor 阶段）：**
- **抽离热路径的类型推断与图片去重**：之前 `AiMessage` 中的 `inferMessageType` (处理大量 includes 检测) 和 `uniqueImages` （Set 对象生成及图片去重）完全在 React 组件的 `useMemo` 或者 render block 中定义，尤其是随着上下文变长、流式逐字 update 时开销会被放大。
- 我们已经将其分离成纯函数架构至 `src/utils/messageUtils.js` (`inferMessageType`, `extractUniqueImages`, `safeParseMetadata`)，使每次 React render 中的引用与对象创建趋向于最简。

**针对 `pretext` 轻量化适配的可行建议**：
1. **纯粹测算预测与分层渲染**：当后续长文本（Markdown）、富代码块导致严重抖动时，可在完成流式输入前使用轻量化 Text 元素兜底，在结束流后再真正走 Markdown parsing/React-Syntax-Highlighter (类似 `pretext` “先布局，后正式装载”的思路)。
2. **纯算术虚拟预估**：`AiMessage` 可在服务器明确返回行高/行数 meta 信息的场景下，跳过 DOM 测量机制预分配高度，减少 React 的 Layout Thrashing。

## 3. 修改文件与交付清单
1. `task.md` (系统追踪)
2. `package.json` 及其 lock 文件
3. `vite.config.js`
4. `src/test/setup.js` [NEW]
5. `src/components/__tests__/ConversationDisplay.test.jsx` [NEW]
6. `src/utils/messageUtils.js` [NEW]
7. `src/components/ConversationDisplay.jsx` [MODIFY]
8. `前端进展汇报.md` [NEW]

## 4. 剩余风险与后续建议
- **遗留风险**：当前流式数据的流控目前依赖后端限速，若瞬间塞入巨大 chunk，仍可能引起 React 长任务阻塞。
- **推荐工作**：为复杂的 `SuggestedQuestions` 与 `OptimizationMessageView` 补充单元测试，提高流式交互中的安全性。

## 5. 测试覆盖与容错机制说明

为了保障系统的稳定性与交互鲁棒性，前端已建立核心渲染链路的自动化测试套件（Vitest + React Testing Library）。

### 5.1 核心测试用例 (5/5 覆盖)
目前所有测试用例均 100% 通过（跑通命令：`npm run test`）。主要验证了以下三个层面的稳定性：
1. **消息路由精确度**：系统能够准确分析后端返回的消息特征，正确地将“普通对话”、“优化过程分析”和“3D建模展示”分发到对应的UI组件中。
2. **大模型失效容错（优雅降级）**：针对 AI 接口可能发生的异常（如由于配额耗尽或网络中断导致仅输出文字、未下发 3D 代码文件），前端不再因缺少依赖文件而陷入无限 Loading 锁死，而是能在秒级识别异常状态，并主动向用户展示降级提示（例如提示文件生成失败），保证系统体验不中断。
3. **排版格式防崩溃保护**：针对当前含有深度思考过程的大模型（其内部通常含有大量非标准 `<think>` 标签及混合代码），前端能够在渲染前级完成格式隔离与清洗。确保大模型凌乱的内部思考标签不会破坏原生的 Markdown 解析树，从而完美保护了核心数据表格和页面布局的呈现逻辑。

### 5.2 机制工程意义
上述机制填补了此前因 AI 大模型输出格式不可控导致的前端白屏、排版错乱等隐患，使得本系统的问答展示层达到了可稳健演示的工业标准。

---

## 6. Provider 切换功能（2026-04-29）

### 6.1 需求背景
后端统一执行接口 `/tasks/execute` 的请求体 `TaskExecuteRequest` 新增了 `provider` 字段：
- `provider: Optional[Literal["agent", "dify"]]`，不填默认使用 `agent`
- `agent`：使用智能体（Agent）调用 SolidWorks 等后台服务
- `dify`：使用 Dify 工作流引擎执行

### 6.2 前端实现
- **新建 `ProviderSelector` 组件**（`src/components/ProviderSelector.jsx`）：
  - 采用 Segmented Control（胶囊切换）设计
  - 两个选项：🤖 Agent（默认）/ 🔗 Dify
  - 选中态有平滑的渐变滑动动画 + 图标缩放效果
  - 支持 `disabled` 状态（流式传输时禁用切换）

- **几何建模页面** (`GeometricModelingPage.jsx`)：
  - 初始视图和工作视图均集成 ProviderSelector
  - `handleSendMessage` 中将 `provider` 字段传入 `executeTaskAPI` 请求体

- **设计优化页面** (`DesignOptimizationPage.jsx`)：
  - 初始上传视图和底部操作栏均集成 ProviderSelector
  - `handleStartOptimization` 中将 `provider` 字段传入请求体

- **任务卡片** (`TaskCard.jsx`)：
  - 当任务数据包含 `provider` 字段时，显示来源标签（紫色 Dify / 灰色 Agent）

- **任务管理页面** (`TaskContainerPage.jsx`)：
  - 预览对话框中新增"执行方式"字段显示

### 6.3 修改文件清单
| 文件 | 操作 | 说明 |
|------|------|------|
| `src/components/ProviderSelector.jsx` | **NEW** | Provider 切换组件 |
| `src/pages/GeometricModelingPage.jsx` | MODIFY | 集成 ProviderSelector + 传参 |
| `src/pages/DesignOptimizationPage.jsx` | MODIFY | 集成 ProviderSelector + 传参 |
| `src/mocks/taskMock.js` | MODIFY | 增加 provider 区分 Mock 响应 |
| `src/components/TaskCard.jsx` | MODIFY | 展示 provider 来源标签 |
| `src/pages/TaskContainerPage.jsx` | MODIFY | 预览对话框增加 provider 信息 |

### 6.4 验证状态
- ✅ `npm run build` 构建通过（0 错误）
- ✅ 已推送至 GitHub（commit: `a40a810`）
- ⏳ 待服务器环境远程联调验证

---

## 7. 前端几何体预览 — 无 STL 文件兜底方案（2026-04-29）

### 7.1 问题背景
当使用 Dify 工作流执行几何建模任务时，后端返回的 `metadata` 中 `stl_file`、`code_file`、`preview_image` 均为 `null`，但 AI 回复文本中包含了参数表（如 `x_length=10, y_length=10, z_length=10`）。原有逻辑因无模型文件而无法渲染 3D 视图。

### 7.2 解决方案
**前端自动解析 AI 回复中的参数表，使用 Three.js 直接生成几何体预览：**

1. **解析参数表**：从 Markdown 表格中提取参数名和推荐值
2. **推断几何体类型**：通过参数签名 + 文本关键词匹配（立方体、圆柱、球体、圆锥、圆环）
3. **构建 Three.js 几何体**：`BoxGeometry` / `CylinderGeometry` / `SphereGeometry` 等
4. **导出为 STL blob**：使用 `STLExporter` 转为 blob URL，直接喂给现有 `ThreeDViewer`

### 7.3 触发条件
```
stl_file === null  &&  isStreaming === false  &&  AI内容长度 > 50字符
→ 调用 generatePreviewFromAIResponse(content)
→ 解析成功则自动显示预览 + 显示橙色「前端预览 · 立方体」标识
```

### 7.4 支持的几何体类型
| 类型 | 参数签名 | 关键词 |
|------|----------|--------|
| 立方体 | `x_length, y_length, z_length` | 立方体、长方体、box、cube |
| 圆柱体 | `radius, height` | 圆柱、cylinder |
| 球体 | `radius`（无 height） | 球体、sphere |
| 圆锥体 | `radius, height` + 关键词 | 圆锥、cone |
| 圆环体 | `major_radius, minor_radius` | 圆环、torus |

### 7.5 修改文件清单
| 文件 | 操作 | 说明 |
|------|------|------|
| `src/utils/geometryBuilder.js` | **NEW** | 参数解析 + Three.js 构建 + STL 导出 |
| `src/pages/GeometricModelingPage.jsx` | MODIFY | 集成前端预览兜底逻辑 + 预览标识 UI |

### 7.6 验证状态
- ✅ `npm run build` 构建通过
- ✅ 已推送至 GitHub（commit: `ac86b9f`）
- ⏳ 待服务器环境联调（需 Dify 返回参数表来触发前端预览）
